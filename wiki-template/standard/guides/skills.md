# Skills

本页是项目最佳实践 skill 的发现目录：每个 skill 一个 section，写明触发场景与必须使用的 skill 名。规范正文保留在对应 skill 包（`.claude/skills/<name>/`）内，本页只负责被 `wiki-researcher` 发现并在计划阶段绑定。

新增或转换最佳实践 skill 时，用 `scaffold-practice-skill` 自动在本页登记 section（不要手写）。空目录页本身没有 section，因此在登记第一个 skill 前不会被选中，这是预期行为。
