---
name: spec-progressive-disclosure
description: Use when a task needs project-specific implementation rules from .superpowers/spec. Read the entry index first, then only the relevant spec files on demand.
---

Generated by superpower-adapter.

Use `.superpowers/spec/` as the project spec source.

Rules:
1. If `.superpowers/current-plan` exists and the corresponding `docs/superpowers/plans/<stem>.context/` sidecar is initialized, treat that sidecar as the first source of truth for selected task context.
2. During planning, start with `.superpowers/spec/index.md`, follow the index chain, and record the minimum relevant spec files into the plan sidecar `plan.jsonl`.
3. During implementation or review, consume the existing sidecar context first instead of re-selecting specs from scratch.
4. Only read fresh spec indexes or leaf files outside the sidecar when the sidecar is missing, invalid, or clearly insufficient for the current task.
5. Do not read the whole `.superpowers/spec/` tree unless the human explicitly asks for a full audit.
6. If a task changes behavior, contracts, or conventions, update the relevant spec through the `update-spec` workflow.

Suggested flow:
- Understand task
- Read `.superpowers/spec/index.md`
- Follow links or listed paths to narrower indexes
- Read the minimum detailed spec files needed to implement or review safely
- After finishing work, update the relevant spec documents if new knowledge should persist
