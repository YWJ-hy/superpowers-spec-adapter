---
name: wiki-progressive-disclosure
description: Use only as a reference or manual fallback for how adapter reads .superpowers/wiki progressively. Normal brainstorming and writing-plans use the wiki-researcher agent directly.
---

Generated by superpower-adapter.

This skill is a reference and fallback guide, not the default wiki selection path.

Default adapter flow:
1. During `brainstorming`, use `wiki-researcher` with `phase: brainstorm` to disclose a small amount of relevant project wiki context.
2. During `writing-plans`, use `wiki-researcher` with `phase: plan` to formally select relevant project wiki.
3. Record selected wiki pages and constraints in the plan's `## Referenced Project Wiki` section.
4. During implementation and review, consume `Referenced Project Wiki` instead of reselecting wiki pages from scratch.
5. After finishing work, use the `update-wiki` skill to review whether durable implementation knowledge should persist in `.superpowers/wiki/`.

Use this skill only when:
- the user asks how progressive wiki disclosure works;
- `wiki-researcher` is unavailable and a manual fallback is needed;
- you are debugging adapter installation or native skill patch behavior.

Reference rules:
1. Start with `.superpowers/wiki/index.md`.
2. Follow links or listed paths to narrower indexes before reading leaf wiki pages.
3. Read only the minimum detailed wiki pages needed for the current phase.
4. Do not read the whole `.superpowers/wiki/` tree unless the human explicitly asks for a full audit.
5. If `.superpowers/wiki/index.md` is missing, report that project wiki is unavailable instead of guessing paths.
6. Treat adapter sidecar JSONL files as diagnostics only, not as the required source of truth for new work.

Manual fallback flow:
- Understand task
- Read `.superpowers/wiki/index.md`
- Follow links or listed paths to narrower indexes
- Read the minimum relevant leaf wiki page files
- If planning, write selected wiki pages into `Referenced Project Wiki`
- If implementing or reviewing, use the plan's `Referenced Project Wiki`
- After finishing work, update relevant wiki pages if new knowledge should persist
