---
description: "Initialize .superpowers/wiki starter knowledge through agent-led analysis of project inventory"
---

Generated by superpower-adapter.

Use this command to help the user initialize `.superpowers/wiki/` from the current project state.

This command is only for the first-pass bootstrap of wiki knowledge.
After implementation work produces new durable knowledge, continue maintaining wiki pages with the `update-wiki` skill.

---

## Goal

Inspect the current project shape, gather a mechanical inventory, then let the agent decide whether lightweight starter wiki content should be written into existing indexed leaf wiki pages.

Python scripts must not invent durable rules, choose final target ownership, or write unverified initialization summaries by default.

## Non-negotiable rules

- Treat this command as initialization only, not ongoing maintenance.
- Treat this as a standalone adapter command, not as a Superpowers development workflow step.
- Do not invoke Superpowers planning, implementation, review, completion, or verification commands/skills before or after this command.
- Do not invoke `superpowers:verification-before-completion` or similar completion checks for this command.
- Do not overwrite existing wiki pages.
- Keep generated content lightweight, explicit about uncertainty, and grounded in observable project structure.
- Prefer appending lightweight starter notes into existing indexed leaf wiki pages over inventing detailed contracts that have not been verified.
- Ask the user when target ownership is ambiguous or when no suitable indexed leaf exists.
- After this command finishes, direct future durable knowledge updates to the `update-wiki` skill.

## User Input

```text
$ARGUMENTS
```

You must consider the user input before proceeding.
Treat it as an optional focus hint for what part of the repository should be prioritized during initialization.

## Workflow

1. Confirm the current repository root.
2. Ensure `.superpowers/wiki/index.md` exists; if not, ask the user to run `bootstrap-wiki` first.
3. Generate mechanical inventory:

```bash
python3 __SUPERPOWER_ADAPTER_PLUGIN_ROOT__/scripts/init-wiki.py . "$ARGUMENTS" --json
```

4. Read `.superpowers/wiki/index.md` and any relevant indexed leaf wiki pages from the inventory.
5. Decide whether starter wiki notes are useful.
6. If writing, edit existing indexed leaf wiki pages directly with lightweight, clearly caveated starter content.
7. Do not write hard contracts, validation matrices, or project conventions unless they are verified from code or user confirmation.
8. Refresh indexed references:

```bash
python3 __SUPERPOWER_ADAPTER_PLUGIN_ROOT__/scripts/update-wiki.py
```

9. Tell the user which files were initialized, which signals were only candidates, and remind them that later knowledge changes belong in the `update-wiki` skill.

## Inventory script output

The initialization script outputs inventory only. It does not write wiki content.

Expected fields include:

- `projectRoot`
- `wikiRoot`
- `focusHint`
- `languages`
- `stackSignals`
- `topDirectories`
- `sampleFiles`
- `indexedWikiPages`
- `warnings`
- `mechanicalOnly`

## Writing guidance

- Keep initialization summaries concise.
- Record only observations that can be justified from project structure, package signals, or file samples.
- Phrase unverified conclusions as likely or candidate areas, not hard facts.
- Do not turn this command into a full architecture analysis.
- Do not force content into every indexed wiki page.
- If no useful starter knowledge is justified, report the inventory and stop without editing wiki pages.
- If the generated output is clearly incomplete, tell the user what should be refined next.

## Completion checklist

- [ ] The inventory script was run.
- [ ] Relevant indexed wiki pages were read before writing.
- [ ] Any written content was agent-selected and grounded in observable project signals.
- [ ] No unverified hard contract was created.
- [ ] Ambiguous ownership was clarified with the user.
- [ ] `update-wiki.py` refreshed the index chain if any content changed.
