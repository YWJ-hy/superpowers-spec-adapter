---
description: "Initialize .superpowers/spec from the current project so the user starts with grounded starter knowledge"
---

Generated by superpower-adapter.

Use this command to help the user initialize `.superpowers/spec/` from the current project state.

This command is only for the first-pass bootstrap of spec knowledge.
After implementation work produces new durable knowledge, continue maintaining specs with `update-spec`.

---

## Goal

Analyze the current project shape, infer likely implementation areas, and write first-pass knowledge back into the existing topic files under `.superpowers/spec/` without overwriting existing user-authored files.

## Non-negotiable rules

- Treat this command as initialization only, not ongoing maintenance.
- Do not overwrite existing spec files.
- Keep generated content lightweight, explicit about uncertainty, and grounded in observable project structure.
- Prefer appending lightweight initialization summaries into existing topic files over inventing detailed contracts that have not been verified.
- After this command finishes, direct future durable knowledge updates to `update-spec`.

## User Input

```text
$ARGUMENTS
```

You must consider the user input before proceeding.
Treat it as an optional focus hint for what part of the repository should be prioritized during initialization.

## Workflow

1. Confirm the current repository root.
2. Ensure `.superpowers/spec/index.md` exists; if not, ask the user to run `bootstrap-spec` first.
3. Run the initialization script:

```bash
python3 __SUPERPOWER_ADAPTER_PLUGIN_ROOT__/scripts/init-spec.py . "$ARGUMENTS"
```

4. Read the generated files that were added during initialization.
5. Briefly validate that the generated content matches the visible repository shape.
6. Tell the user which files were initialized and remind them that later knowledge changes belong in `update-spec`.

## Expected outputs

The command may append initialization blocks into:

- `.superpowers/spec/index.md`
- leaf spec files that are reachable from the `.superpowers/spec/index.md` index graph

It should not invent fixed directories. It should refresh indexed references through `update-spec.py`.

## Writing guidance

- Keep initialization summaries concise.
- Record only observations that can be justified from project structure, package signals, or file samples.
- Phrase unverified conclusions as likely or candidate areas, not hard facts.
- Do not turn this command into a full architecture analysis.
- If the generated output is clearly incomplete, tell the user what should be refined next.
